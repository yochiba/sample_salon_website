.services
  h2 サービス一覧
  / = form_with model: @search_services, local: true do |f|
  /   = f.text_field :keyword
  /   = f.submit "検索"
  // カテゴリーごとに表示できるようにする
  .service-container
    table
      tr
        th サービスカテゴリー
        th サービス内容
        th 施術時間
        th 金額
      - @service_info.each do |service|
        tr
          td = service.servicecategory
          td = link_to "#{service.servicecategory}", service_detail_path(service.servicecategory)
          td = "#{service.servicetime}分"
          td = "#{service.serviceprice}円"